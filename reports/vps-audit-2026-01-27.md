# 🖥️ Auditoria VPS - 27 Janeiro 2026

## ✅ MIGRAÇÃO COMPLETA

A migração do Mac para a VPS foi **bem sucedida**!

| Item | Estado |
|------|--------|
| Clawdbot Gateway | ✅ A correr (PID 31056) |
| Telegram | ✅ Funcional |
| Memória sincronizada | ✅ Completa |
| Projectos | ✅ 8 projectos |
| GitHub | ✅ Conectado |

---

## 📊 RECURSOS ACTUAIS

### Hardware (OVH VPS)
| Recurso | Specs | Uso Actual |
|---------|-------|------------|
| CPU | 6 vCore (Haswell) | ~25% (1 core) |
| RAM | 12 GB | 1.1 GB (9%) |
| Disco | 100 GB NVMe | 4.3 GB (5%) |
| Rede | 1 Gbps ilimitado | ✅ |

### Avaliação: **MUITO BEM DIMENSIONADO** 
Tens margem enorme para crescer. Não precisas de upgrade por agora.

---

## 🔴 PROBLEMAS IDENTIFICADOS

### 1. SEGURANÇA (CRÍTICO)
- ❌ **UFW (Firewall) não configurado** - VPS exposta
- ❌ **Fail2ban não instalado** - Sem proteção brute-force
- ❌ **SSH na porta 22** - Porta padrão (mais ataques)
- ❌ **Password auth activo** - Melhor só chaves SSH

### 2. PERSISTÊNCIA
- ⚠️ **Clawdbot não está como serviço systemd** - Se reiniciar, não arranca
- ⚠️ **Docker não instalado** - Coolify não funciona

### 3. MONITORIZAÇÃO
- ❌ Sem monitorização de recursos
- ❌ Sem alertas de disco/CPU/RAM
- ❌ Sem backups automáticos

### 4. SKILLS
- ❌ Nenhuma skill extra instalada

---

## 🟢 RECOMENDAÇÕES

### PRIORIDADE 1: Segurança (Fazer AGORA)

```bash
# 1. Instalar UFW + Fail2ban
sudo apt update && sudo apt install -y ufw fail2ban

# 2. Configurar Firewall
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow 22/tcp comment 'SSH'
sudo ufw allow 8000/tcp comment 'Coolify'
sudo ufw enable

# 3. Activar Fail2ban
sudo systemctl enable fail2ban
sudo systemctl start fail2ban
```

### PRIORIDADE 2: Persistência

```bash
# Criar serviço systemd para Clawdbot
mkdir -p ~/.config/systemd/user
cat > ~/.config/systemd/user/clawdbot.service << 'EOF'
[Unit]
Description=Clawdbot Gateway
After=network.target

[Service]
Type=simple
Environment=PATH=/home/ubuntu/.nvm/versions/node/v22.22.0/bin:/usr/bin
ExecStart=/home/ubuntu/.nvm/versions/node/v22.22.0/bin/clawdbot gateway
Restart=always
RestartSec=10

[Install]
WantedBy=default.target
EOF

systemctl --user daemon-reload
systemctl --user enable clawdbot
systemctl --user start clawdbot
loginctl enable-linger ubuntu
```

### PRIORIDADE 3: Skills Recomendadas

| Skill | Para Quê | Prioridade |
|-------|----------|------------|
| `gog` | Gmail, Calendar, Drive | ⭐⭐⭐⭐⭐ |
| `github` | Issues, PRs, CI | ⭐⭐⭐⭐⭐ |
| `notion` | Gestão projectos | ⭐⭐⭐⭐ |
| `himalaya` | Email IMAP/SMTP | ⭐⭐⭐⭐ |
| `weather` | Previsão tempo | ⭐⭐⭐ |
| `slack` | Se usares Slack | ⭐⭐⭐ |

### PRIORIDADE 4: Monitorização

```bash
# Instalar htop + ncdu
sudo apt install -y htop ncdu

# Para monitorização avançada (opcional):
# - Netdata (dashboard web)
# - Uptime Kuma (alertas)
```

### PRIORIDADE 5: Backups

```bash
# Backup diário do workspace para outro local
# Opções:
# - rsync para outro servidor
# - rclone para Google Drive/S3
# - Restic para backups encriptados
```

---

## 📋 CHECKLIST DE HARDENING

- [ ] UFW activo com regras mínimas
- [ ] Fail2ban a proteger SSH
- [ ] SSH só com chaves (desactivar password)
- [ ] Mudar porta SSH (ex: 2222)
- [ ] Clawdbot como serviço systemd
- [ ] Backups automáticos configurados
- [ ] Monitorização básica (htop)
- [ ] Skills essenciais instaladas

---

## 🚀 PRÓXIMOS PASSOS SUGERIDOS

1. **AGORA:** Executar comandos de segurança (5 min)
2. **HOJE:** Configurar serviço systemd (10 min)
3. **ESTA SEMANA:** Instalar skills prioritárias
4. **ESTE MÊS:** Configurar backups + monitorização

---

## 💰 SOBRE UPGRADE

**Não precisas de upgrade agora.** Estás a usar:
- 9% da RAM
- 5% do disco
- ~25% de 1 core

Considera upgrade SE:
- Quiseres correr muitos containers Docker
- A RAM passar dos 80%
- O disco passar dos 70%

---

*Relatório gerado: 2026-01-27 22:35 UTC*
